spring:
  datasource:
    url: jdbc:mysql://database-fitgym.cpcio88easmg.us-east-2.rds.amazonaws.com:3306/fitgym_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: admin
    password: acfse15kpSA
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update #De hibernate - actualiza tabla automaticamente
    show-sql: true
    properties:
      hibernate:
      ddl-auto=update:
        dialect: org.hibernate.dialect.MySQLDialect